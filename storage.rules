rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access to all shop images
      allow read: if true;
      
      // Allow authenticated users to upload images
      allow write: if request.auth != null;
    }
  }
}
